{
  "name": "rabbitmq",
  "version": "1.0.0",
  "description": "```bash pip3 install -r requirements.txt ```",
  "main": "fanout_emit_log.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "amqplib": "^0.8.0"
  }
}
